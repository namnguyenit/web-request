<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Logs nhận được</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <style>
    body { padding: 20px; }
    table { font-size: 14px; }
    .ip { font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace; }
  </style>
</head>
<body>
  <div class="container">
    <div class="d-flex align-items-center justify-content-between mb-3">
      <h2 class="m-0">Danh sách yêu cầu đã nhận</h2>
      <div>
        <a class="btn btn-outline-secondary btn-sm" href="/">Làm mới</a>
        <a class="btn btn-outline-danger btn-sm" href="/clear" onclick="return confirm('Xóa toàn bộ log?');">Xóa log</a>
      </div>
    </div>
    <div class="card">
      <div class="card-body">
  <div class="mb-2 text-muted">Tổng số: <strong><%= (logs && logs.length) ? logs.length : 0 %></strong></div>
  <div class="table-responsive">
          <table class="table table-striped table-bordered align-middle">
            <thead class="table-dark">
              <tr>
                <th scope="col" style="width: 80px;">STT</th>
                <th scope="col" style="width: 220px;">Địa chỉ IP</th>
                <th scope="col">Giá trị</th>
                <th scope="col" style="width: 220px;">Thời gian</th>
              </tr>
            </thead>
            <tbody>
            <% if (logs && logs.length) { %>
              <% logs.slice().reverse().forEach(function(row) { %>
                <tr>
                  <td><%= row.no %></td>
                  <td class="ip"><%= row.ip %></td>
                  <td><%= row.value %></td>
                  <td><%= row.time %></td>
                </tr>
              <% }); %>
            <% } else { %>
              <tr>
                <td colspan="4" class="text-center text-muted">Chưa có dữ liệu</td>
              </tr>
            <% } %>
            </tbody>
          </table>
        </div>
        <div class="mt-3 text-muted">
          Gửi thử: <code>http://localhost:3000/====abc123</code> rồi reload trang này.
        </div>
      </div>
    </div>
  </div>
</body>
</html>
